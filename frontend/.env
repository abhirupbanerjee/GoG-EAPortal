# ============================================
# FRONTEND ENVIRONMENT CONFIGURATION
# ============================================
# These variables are populated from root .env via docker-compose

# API Endpoints (public URLs)
API_BASE_URL=${API_BASE_URL}
IMAGE_BASE_URL=${IMAGE_BASE_URL}
MINIO_BASE_URL=${MINIO_BASE_URL}

# Application Configuration
NEXT_PUBLIC_APP_NAME=Government of Grenada Portal
NEXT_PUBLIC_ORGANIZATION=${ORGANIZATION_NAME}